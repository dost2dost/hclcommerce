const appState ={
    url :    'url imported',
    Store: '',
    Catalog: ''
}

const appSettings = (getAppState = appState, action) =>{
    //console.log(action);
     
    return getAppState;
}
export default appSettings;